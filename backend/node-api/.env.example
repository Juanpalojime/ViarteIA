# ===========================================
# 🔑 CLAVES DE API Y SECRETOS (NUNCA subas valores reales)
# ===========================================

# Configuración del Servidor
PORT=3001
# Ruta relativa a la DB (SQLite)
DATABASE_URL=dev.db

# Groq (Llama 3 para Magic Prompt y Asistente IA)
# Obten tu clave en: https://console.groq.com/keys
GROQ_API_KEY=tu_clave_de_groq_aqui

# JWT Secret (genera uno aleatorio de 64+ caracteres para seguridad)
# Ej: openssl rand -hex 32
JWT_SECRET=tu_secreto_jwt_aleatorio_de_64_caracteres

# Conexión con otros servicios
PYTHON_API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Tokens Opcionales - Solo si usas estas funcionalidades
NGROK_AUTH_TOKEN=
HF_TOKEN=
